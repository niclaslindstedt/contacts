# Local build knobs for the native wrapper. Copy to `native/.env` (gitignored,
# and kept out of the EAS upload by the root .easignore) and fill in what you
# need. Nothing here is required for a normal build.

# The EAS project this app builds under. `eas init` prints the id but cannot
# write it into a dynamic config (app.config.js), so it is passed in instead.
# CI reads it from the EAS_PROJECT_ID repository secret.
# WHERE: https://expo.dev -> the project -> its overview page, or `eas init`.
# SHAPE: a lowercase UUID, 8-4-4-4-12.
# EAS_PROJECT_ID=

# An Expo access token, for driving EAS with no interactive login. A laptop
# uses `eas login` instead; CI reads it from the EXPO_TOKEN repository secret.
# WHERE: https://expo.dev/settings/access-tokens (personal), or
# https://expo.dev/accounts/<account>/settings/access-tokens (robot — the right
# kind for CI, since it cannot sign in to the dashboard and owns no projects).
# Treat it as a password.
# EXPO_TOKEN=

# Points the WebView at a deployed slot instead of the copy of the app bundled
# inside it. DEBUGGING ONLY — a store build that streams the website is the
# exact shape App Store guideline 4.2 rejects.
# VALUE: any URL the device can reach — a deploy slot on the live origin, or a
# dev server on the LAN (http://<your-lan-ip>:5173/, not localhost, which on a
# phone means the phone).
# EXPO_PUBLIC_CONTACTS_URL=https://contacts.niclaslindstedt.se/preview/

# ---------------------------------------------------------------------------
# THE STORE LISTING (make store-metadata / make store-upload)
# ---------------------------------------------------------------------------

# Which App Store Connect app the listing is uploaded to — the same identity
# every build takes. Also read by `make store-metadata` for the review notes'
# cross-checks.
# SHAPE: reverse-DNS, e.g. se.example.contacts
# APP_BUNDLE_ID=

# The listing's name on the product page. Unset, the plain project name.
# APP_DISPLAY_NAME=

# An App Store Connect API key, for `make store-upload` (fastlane deliver).
# WHERE: App Store Connect -> Users and Access -> Integrations -> App Store
# Connect API -> a key with the App Manager role. The .p8 downloads ONCE.
# ASC_KEY_ID=
# ASC_ISSUER_ID=
# Either the path to the .p8, or its contents.
# ASC_KEY_PATH=
# ASC_KEY_CONTENT=

# The phone number App Store review rings. Never committed: this repository
# is public. The generator leaves the field out rather than upload a
# placeholder, and `make store-preflight` fails until it is set.
# SHAPE: international, e.g. +46 70 000 00 00
# ASC_REVIEW_PHONE=
