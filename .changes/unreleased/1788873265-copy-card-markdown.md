---
type: Changed
title: Copy a card as Markdown
---

The copy button on a contact card now copies its information as Markdown — the read view's sections, in your own formats — instead of a vCard block.
