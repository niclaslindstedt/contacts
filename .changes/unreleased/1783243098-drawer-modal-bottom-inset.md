---
type: Fixed
title: Remove redundant bottom safe-area gap under the sidebar footer and modal buttons
---

Installed as a PWA, the app already sits above the home-indicator safe area, so
the extra bottom inset it reserved just showed up as an empty gap. The side
menu's footer (Donate / About / Settings) and a modal's bottom button row (for
example Settings' **Reset / Cancel / Save**) now sit snug against the bottom
instead of floating above a dead strip.
