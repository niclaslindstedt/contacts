---
type: Changed
title: Imports merge duplicates
doc: import
---

Importing contacts now merges duplicates instead of adding copies — a shared phone or email merges silently, a matching name asks first (with an "All (n)" shortcut), and merging only ever fills in what's missing.
